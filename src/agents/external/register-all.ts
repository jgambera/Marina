/**
 * Register all external agent adapters.
 * Import this module once at startup to make them available in the registry.
 */

import "./adapters/claude-code";
import "./adapters/goose";
import "./adapters/codex-cli";
